#include <string>

class Invoice{
public:
    explicit Invoice(std::string invoiceNumber, std::string invoiceDescription, int invoiceQuantity, int invoicePrice, double invoiceVAT, double invoiceDiscount) : 
        name{accountName} {
            if(initialBalance > 0){
private:
};